1.创建一个函数的执行环境，并将其放在执行环境栈的栈顶。（栈是一种数据结构，类似数组。特点是后进先出）
    -栈顶纵使当前执行的环境
2.创建一个新的变量对象，并将变量对象放到作用域的栈顶。（变量对象时用来保存变量的对象，无法访问，功能类似于window）
3.预解析，将使用var声明的变量添加到变量对象中，将函数声明创建的函数创建好添加到变量对象中。
4.逐行执行函数中的代码
5.函数执行完毕，将变量对象从环境栈中弹出并销毁
